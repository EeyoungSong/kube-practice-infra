apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kube-practice-app
  namespace: argocd
spec:
  project: default # ✅ 필수 항목 추가
  source:
    repoURL: "https://github.com/EeyoungSong/kube-practice-infra.git" # 👉 Infra Repo 주소
    targetRevision: main
    path: k8s # 👉 YAML이 있는 폴더 (ex: infra/k8s)
  destination:
    server: "https://kubernetes.default.svc"
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
